#include <stdio.h>
int main()
{
    long long int A;
    long long int B;
    scanf("%lld %lld", &A, &B);
    // multiplication
    long long int multiply = A * B;
    printf("%lld", multiply);

    return 0;
}